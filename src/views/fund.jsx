import React from "react";
import styled from "styled-components";

const Fund = () => <Container>menu</Container>;

const Container = styled.div`
  display: flex;
  padding: 24px;
  border-radius: 8px;
  min-height: calc(100vh - 180px);
  background: #fcfcfc;
`;
export default Fund;
